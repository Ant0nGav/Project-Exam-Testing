<Window x:Class="Вид.FioInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Вид"
        mc:Ignorable="d"
        Title="Создание варианта для участника" 
        Height="500" 
        Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Информация о путях -->
        <Border Grid.Row="0" Background="LightBlue" Margin="0,0,0,10" Padding="5">
            <StackPanel>
                <TextBlock Text="Задания будут сохранены в:" FontWeight="Bold"/>
                <TextBlock x:Name="txtTasksPath" Text="C:\Users\1\Desktop\ubn\Project-Exam-Testing\Вид\Вид\Users_Tasks"/>
                <TextBlock Text="Ответы будут сохранены в:" FontWeight="Bold" Margin="0,5,0,0"/>
                <TextBlock x:Name="txtAnswersPath" Text="C:\Users\1\Desktop\ubn\Project-Exam-Testing\Вид\Вид\Users_answers"/>
            </StackPanel>
        </Border>

        <!-- ФИО -->
        <Label Grid.Row="1" Content="ФИО участника:" Margin="0,0,0,5"/>
        <TextBox x:Name="txtFullName" Grid.Row="2" Margin="0,0,0,10" Height="25"/>

        <!-- Путь к папке с вариантами -->
        <Label Grid.Row="3" Content="Путь к папке с вариантами:" Margin="0,0,0,5"/>
        <Grid Grid.Row="4" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtVariantsPath" Grid.Column="0" Height="25" IsReadOnly="True"/>
            <Button x:Name="btnBrowseVariants" Grid.Column="1" Content="Обзор..." 
                    Margin="5,0,0,0" Width="80" Click="btnBrowseVariants_Click"/>
        </Grid>

        <!-- Выбор варианта -->
        <Label Grid.Row="5" Content="Выберите вариант:" Margin="0,0,0,5"/>
        <ComboBox x:Name="cmbVariants" Grid.Row="6" Margin="0,0,0,15" Height="25"/>

        <!-- Лог выполнения -->
        <Label Grid.Row="7" Content="Лог выполнения:" Margin="0,0,0,5"/>
        <TextBox x:Name="txtLog" Grid.Row="7" Margin="0,25,0,0" IsReadOnly="True" 
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCreate" Content="Создать" Width="100" Height="30" 
                    Margin="0,0,10,0" Click="btnCreate_Click"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="100" Height="30" 
                    Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
